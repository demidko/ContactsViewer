const SearchBox = document.getElementById('search-box') as HTMLInputElement

function GenerateTrueRequest(request: string): string {

    const lexemes = SearchBox.value.split(' ').filter()
   
    return 's'
}

function SuperActualPath() {
    SearchBox.value = GenerateTrueRequest(SearchBox.value.toUpperCase())
}

function Search() {
    SuperActualPath()
    const request = SearchBox.value.toUpperCase()

}


